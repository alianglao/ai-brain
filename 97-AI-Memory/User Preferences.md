# User Preferences

研究方向：数学、教育、历史、建模、AI。输出：公众号、网页。偏好直接执行，保留已有内容。

## Agent 审查偏好

- Executor 与 Reviewer 必须使用不同模型；当前执行模型不得担任自己的监工。
- 监工模型只使用 `gpt-5.5` 或 `gpt-5.4-mini`。
- 默认优先 `gpt-5.5`；低额度任务使用 `gpt-5.4-mini`。
