开始工作：请先读取 “C:\Users\18928\Desktop\ai brain” 根目录的 AGENTS.md，并按照其中的规则，仅按当前任务需要读取少量相关记忆，然后直接开始处理。

结束并沉淀：只提取本次会话中长期有效、已确认或已验证的信息，去重后更新在““C:\Users\18928\Desktop\ai brain” ”最合适的 Memory、Context 或 Project 文件。简单对话不保存，默认不保存逐字聊天记录。


读取 C:\Users\18928\Desktop\ai brain 根目录的 AGENTS.md，并按照其中的规则只提取本次会话中长期有效、已确认或已验证的信息，去重后更新最合适的 Memory、Context 或 Project 文件。简单对话不保存，默认不保存逐字聊天记录。