# 无线电干扰源自动定位项目 2026-B — Project Status

## 当前状态
建模与论文图件整理中。当前优先级是先完成数学建模和文档整理，再写伪代码，最后编写算法代码；不得擅自改变题目条件、参数、单位、坐标系或目录结构。

## 已完成事项
- 2026-09-11 已完成项目初步盘点：项目根目录位于 `C:\Users\18928\Desktop\数学建模研究室\projects\无线电干扰源自动定位项目 2026-B`。已读取项目根目录 `README.md`、`项目说明与开发规范.md`、题面 PDF、附件和问题目录；官方条件已核对为目标圆半径 1800 m、测向误差 ±1°、有效接收距离 1000–1500 m、5 m 内不能示向、20 m 内清除、移动速度 5 m/s、检测 5 s、切换信道 1 s、清除 5 s。
- 2026-09-11 问题一已形成交会定位区域模型：单次测向约束、多次可行域交集、圆弧多边形化、凸包直径与旋转卡壳、最小覆盖圆边界说明。文件：`问题一_交会定位模型\模型说明.md`。
- 2026-09-11 问题二已形成鲁棒第二检测点模型：第一次测向可行域 `Ω1`、可靠第二测点区域 `ΩD`、最坏定位直径 `J(S2)`、最优点 `S2*`、近最优候选区域 `ΩC(5%)`、边界点对等价目标函数和稳健性验证口径。文件：`问题二_检测点优化模型\鲁棒第二检测点模型.md`、`问题二_检测点优化模型\标准化热力图说明.md`。
- 2026-09-11 已生成问题二标准化热力图和姊妹收缩图。输出目录：`问题二_检测点优化模型\figures\output`。主图文件：`problem2_second_detection_heatmap_standardized.png/svg/pdf`；姊妹图文件：`problem2_s2star_contraction_diagram.png/svg/pdf`；数据：`problem2_second_detection_heatmap_standardized.csv`；报告：`问题二标准化热力图验证报告.md`。
- 2026-09-11 热力图已按论文图要求修订：`Ω1` 加描边避免被热力图遮盖；叠加 `J=150/300/600 m` 等值线；保留双星标；图题写明 `S2=(a,b)`、网格 `41×49` 和边界采样 `561/281/81`；图注要点为“最优解为镜像对 `(a*, ±b*)`，且饱和在接收保证边界上”。姊妹图展示 `Ω1 → Ω2` 的直径收缩。
- 2026-09-11 数值验证结果：标准化局部坐标下 `D(Ω1)=1495.003056 m`，局部加密最优点之一为 `(876.2, 480.0) m`，镜像点为 `(876.2, -480.0) m`，`J*=108.638040 m`，图面最大值 `1495.001187 m` 未超过 `D(Ω1)`。SVG 主图保留 45 个 `<text>`，姊妹图保留 24 个 `<text>`，脚本语法检查通过。

## 待办事项
- 继续补齐问题三、问题四的数学模型、伪代码和验证口径。
- 若题目或模拟器给出具体首次检测点 `S1` 与首次示向度，需要按实际全局圆边界重新计算问题二热力图，不能把标准化图写成任意场景的唯一精确解。
- 模拟器接口文档仍需核对与整理；未经确认不要直接生成模拟器控制代码。

## 下一步
- 最适合继续做的是把问题二模型和两张图写入论文正文：说明标准化坐标、可靠检测域、最坏直径目标、镜像最优解和接收保证边界饱和，再补一段边界场景适用性说明。

## 风险
- 标准化热力图只在 `Ω1` 未被 1800 m 全局目标圆边界截断时严格通用；首次检测点靠近边界且示向朝外时，`Ω1`、`ΩD`、`J(S2)`、`S2*` 和 `ΩC` 都可能改变。
- 当前最优点和候选区域是边界离散与有限网格下的数值近似，不是解析精确解；局部网格从 25 m、10 m 到 5 m 时最优点仍有约 20 m 量级的位置变化。
- 项目要求图表、报告和结论保持中文；代码变量、脚本名和标准输出文件名可保留英文以保证复现。

## 决策
- 当前阶段只沉淀建模、文档和可复现图件；算法控制代码必须等模型、伪代码和接口信息确认后再写。
- 问题二没有具体 `S1` 和示向度时，采用标准化局部坐标 `S1=(0,0)`、首次示向度 `0°` 绘制 `J(a,b)`，并在图注与报告中明确不是唯一全局解。
